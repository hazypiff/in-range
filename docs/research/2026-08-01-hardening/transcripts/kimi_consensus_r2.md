• Two disputed items. I need to check the actual cited files myself this time (my refutations came from a delegate that apparently looked at the wrong buffer/file).

  1. H-PRIV-1: check SubtleWakeCoordinator.swift in the repo (main? or worktree? The finding is about iOS — the W5 worktree is the branch, but SubtleWakeCoordinator exists on main too presumably, since AppDelegate references it — I saw AppDelegate.swift:22 `SubtleWakeCoordinator.shared.bootFromPersistence()` in the worktree). Check the cited lines: :21, :22, :407-408, :442-443, :342, and the Dart drainBufferedWakes + isSupported + AppConfig.subtleWake default false.

  2. H-RT-3: check _ingestForeignSample in beacon_service.dart (main repo), range_estimator.dart:75 addObservation signature, and _lastForeignScanAt.

  Let me find the files first. SubtleWakeCoordinator — check both main repo ios/Runner and worktree. The finding report context: H-PRIV-1 in the main report — panel reviewed main @ f2f948e and W5 branch. I'll check main repo first; if file exists there, that's the target.

• Both disputes hinge on exact files/lines — I'll read them myself this time rather than delegate.

BackgroundLocationCoordinator.swift  7.1K
SubtleWakeCoordinator.swift  17.9K
BackgroundLocationCoordinator.swift  7.1K
SubtleWakeCoordinator.swift  17.9K
• Confirmed for H-RT-3: `addObservation` at range_estimator.dart:75-85 takes no timestamp parameter; it stamps `final now = _now()` at :79 and stores `_Sample(now, ...)`. So any observation ingested — including drained native-buffer samples — is stamped with drain time, not capture time. Now I need to confirm _ingestForeignSample calls addObservation without time and sets _lastForeignScanAt = DateTime.now(). Agent 4 said replayed samples enter rangeEstimator.addObservation at :2191 and onSighting at :2312. Let me read _ingestForeignSample around :2150-2320 to verify the exact claims: forwards `at` to _recordLocalSighting, calls addObservation (no time), sets _lastForeignScanAt = DateTime.now().

  Also check the scan watchdog usage of _lastForeignScanAt to substantiate "hides a dead scanner."

  Then SubtleWakeCoordinator.swift: read the cited lines :15-30, :335-350, :400-450. And the Dart side drainBufferedWakes + isSupported + AppConfig.subtleWake default. Let me grep for drainBufferedWakes in lib/.H-RT-3 mechanism confirmed: `_ingestForeignSample` at :2191-2199 calls `rangeEstimator.addObservation` with no timestamp — estimator stamps `_now()` internally (range_estimator.dart:79,85). `_lastForeignScanAt = DateTime.now()` at :2220, ignoring `at`. So the coordinator's dispute #2 is correct: the timestamp survives the drain but is discarded at the classifier boundary. The upload path gets true capture time (`:2268`).

  Now, what does this actually affect?
  - The tier classifier (`rangeEstimator.classify`) treats a 20-min-old drained sample as current → affects `estimatedBand` used for uploadRange (:2255-2258) and for `onSighting` band (:2306-2316). So the local feed ("Close By") and the uploaded band reflect the sample as if fresh.
  - The scan watchdog: check what reads `_lastForeignScanAt`. Let me grep to confirm the watchdog claim ("hides a dead scanner").

  But wait — severity assessment: the finding H-RT-3 says "replayed into the live 90s window with fresh timestamps → false 'Close By' for a peer who was near 20 minutes ago, and it hides a dead scanner from the watchdog." The mechanism as stated ("fresh timestamps") was wrong for the upload path but right in effect for the estimator — the estimator stamps now() which is equivalent to fresh timestamps at the classifier. So the coordinator is right that my refutation was wrong: I refuted the drain, but the finding's effect is real at the estimator boundary. My delegate even noted this ("replayed samples do enter rangeEstimator.addObservation... at drain time, so the estimator window and local feed treat them as live") but called it "deliberate (late-evidence design per :311-316)". Is it deliberate? Lines :311-316 are about the native buffer design. The late-evidence design (0053) is about the *server* accepting true capture times. The estimator is an on-device 90-second sliding window classifier — there is no design rationale for stamping drained samples as now; the estimator simply lacks a timestamp parameter. The comment at :2265-2267 shows the authors cared about true capture time for upload. So the estimator stamping looks like an unconsidered gap, not deliberate design. But is there a counterargument? When the app is killed/locked and iOS buffers sightings natively, on drain (app wake), should a 20-min-old sample make the peer show as "Close By" now? No — that's a false live proximity signal. And `_lastForeignScanAt` — check the watchdog.

  Let me grep _lastForeignScanAt usage.

  Also for H-PRIV-1, need to read SubtleWakeCoordinator.swift:335-350 (removeObject in ack path), :400-450 (lat/lon entries), and check Dart side drainBufferedWakes/isSupported/AppConfig.subtleWake default. Let me read those and grep the Dart side.Now check the emitWake buffer-append path and cap enforcement (around :300-330 presumably), and the Dart side: drainBufferedWakes + isSupported + AppConfig.subtleWake default. Let me read SubtleWakeCoordinator.swift around 290-330 (emitWake) and grep the Dart side.Now I have what I need for both disputes. Let me check the remaining piece for H-PRIV-1: does `drainBufferedWakes` return early unless `isSupported`, and does `isSupported` require `AppConfig.subtleWake`? The grep shows `_readSubtleWakeFlag()` returns AppConfig.subtleWake (default false — app_config.dart:146-150, empty → 'false'). Need to see where isSupported gates the drain. Let me check subtle_wake_service.dart around isSupported and the start of drainBufferedWakes — line 306-330. The drain at :307 checks platform iOS only; does it check _running/isSupported? The drain is called from start() (:159) and onWakeBuffered (:244). If subtleWake flag is false, does start() return early before reaching :159? And does onWakeBuffered get delivered if native side buffers? The native side only buffers if the coordinator is running — but the coordinator's start is driven by Dart... Actually the native SubtleWakeCoordinator.bootFromPersistence() runs pre-Dart from AppDelegate (:22) — persisted wantsToRun state can restart SLC monitoring natively even if the Dart flag is now false! That's the H-DIAG-3-adjacent path: native wakes appendBuffer events natively (emitWake → appendBuffer when no channel or suspended). Then buffer accumulates. When does it ever get cleared? Only via Dart drain+ack (ackBuffer at :335-346, removeObject only when buffer empties). Dart's drain requires... let me verify the isSupported gate inside SubtleWakeService — check lines ~100-160 and where drainBufferedWakes checks isSupported. The coordinator claims "Dart's drainBufferedWakes returns early unless isSupported, which requires AppConfig.subtleWake (default false)". Let me verify.

  Also for precision: are these SLC/CLVisit coordinates "place-level"? SLC (significant location change) fixes are typically cell-tower triangulation — accuracy often hundreds of meters to km; CLVisit coordinates are place-level. The entries carry acc field. The coordinator concedes "raw GPS" → "place-level coordinates". Fair. But note didUpdateLocations uses the SLC manager's fix with whatever accuracy; not "raw precise GPS" since no continuous session (:400 comment "SLC fixes only — this manager never runs a continuous session").

  So H-PRIV-1 adjudication: my refutation cited the wrong buffer (BackgroundLocationCoordinator). The finding's actual subject, SubtleWakeCoordinator, does have: bufferKey "io.inrange.subtlewake.buffer" (:21), cap 50 (:22), lat/lon entries (:407-408 slc, :442-443 visit), removal only in ackBuffer (:335-346), and notifyBufferReady only tells Dart when channel exists + app active. If the Dart flag is off, start() never runs → does drain ever happen? onWakeBuffered is only invoked by native when channel != nil and app active; channel is set when Dart registers the handler (SubtleWakeService constructor/init?). If the Dart service registers a method handler regardless of flag, then onWakeBuffered → drainBufferedWakes; if drain returns early due to isSupported false, buffer never acked, never cleared → persists indefinitely (cap 50, count-only eviction). So the finding stands with modification: cap 50 not "up to 50"... actually report said "up to 50" — correct! Report said "persists up to 50 raw GPS fixes in UserDefaults with a count cap and no age limit, and no path ever clears it — survives updates, included in backups." Cap 50 ✓, count cap ✓, no age limit ✓. "No path ever clears it" — needs the isSupported check. Let me verify the early return.

  Also note: even with the flag ON, UserDefaults is included in backups and survives updates; entries do carry place-level coords. And when flag OFF but native pre-Dart boot re-arms SLC (persisted wantsToRun), buffer grows and is never drained. Need to verify isSupported gate.

  Let me read subtle_wake_service.dart around isSupported and drainBufferedWakes start.One more check: does `drainBufferedWakes` itself check isSupported? Lines 306-330 showed: `:307 if (_platform != TargetPlatform.iOS) return;` then drain-in-flight logic, then invokes channel. Does it check isSupported anywhere? The grep for isSupported showed only :98, :146, :215. So drainBufferedWakes does NOT check isSupported directly — it checks platform only. But wait, does handleWake get called per drained wake, and does the ack happen regardless? Need to see the rest of drainBufferedWakes (lines 306-345) to determine whether the ack lands when isSupported is false. If drain pulls the buffer and acks even with flag off (handleWake returns early but ack still sent), then the buffer IS cleared whenever Dart runs the service on iOS regardless of flag. But drain is only called from start() (:159 — gated by isSupported at :146) and onWakeBuffered (:244 — is the channel handler registered only when running?). If the method handler is registered in the constructor or in start() after the isSupported check, then with flag off: handler never registered → native notifyBufferReady's invokeMethod gets nothing → no drain → buffer persists. Also, does native even buffer when flag off? Native pre-Dart boot (AppDelegate:22 bootFromPersistence) re-arms SLC from persisted wantsToRun — yes, natively, regardless of the Dart flag. emitWake appends to buffer when channel is nil or app suspended. So with flag off but a prior session having run with wantsToRun persisted... hmm, but if flag was always off, native start() was never called by Dart, so wantsToRun never persisted true. The stale-state scenario: bench build had flag on → wantsToRun persisted → later build/flag off → native re-arms SLC pre-Dart (H-DIAG-3 territory) → buffers wakes → Dart flag off → start() returns at :146 → drain never called from there; onWakeBuffered handler — registered where? Let me check where the method call handler is set (_channel.setMethodCallHandler) — likely in start() or an init. If in start() after :146, then with flag off there's no handler and no drain ever → buffer persists forever (capped at 50, FIFO).

  Let me read drainBufferedWakes fully (306-350) and find setMethodCallHandler.Key facts now established for H-PRIV-1:

  - The Dart method handler is registered in the **constructor** (:78), not gated by isSupported. So `onWakeBuffered` → `drainBufferedWakes()` (:244) fires regardless of the flag.
  - `drainBufferedWakes` itself (:306-346) checks only platform, NOT isSupported. It pulls the buffer, calls `handleWake` (which returns early if !isSupported, :215) but still increments `handled` and **acks** (:338). So if Dart runs at all on iOS, the buffer IS drained and acked even with the flag off — handleWake does nothing, but ackBuffer still clears entries.

  Wait — that refutes the coordinator's claim that "Dart's drainBufferedWakes returns early unless isSupported." Looking at the code: drainBufferedWakes does NOT check isSupported. It returns early only on non-iOS. So on iOS, whenever the native side calls onWakeBuffered (app active, channel non-nil — channel is set when? Native side: `channel` property — set via attachChannel presumably called from native start or from Dart's start invoking a native method). Hmm. If the flag is off, Dart never calls native start, so the native `channel` may be nil → notifyBufferReady guard `guard let ch = channel` fails → no onWakeBuffered → no drain. Also appDidBecomeActive → notifyBufferReady also requires channel.

  But does the native coordinator buffer at all when the flag is off? Native buffering happens in emitWake, which fires from SLC/region/visit delegates — those only run if the native coordinator is monitoring, which requires native start (from Dart, gated by flag) OR bootFromPersistence with persisted wantsToRun=true (which could only be true if a previous session had the flag on). So in the pure flag-off world, no buffer accumulates. In the stale-state world (flag was on, wantsToRun persisted, then flag off / different build), native re-arms monitoring pre-Dart, buffers events... and the native channel: is it attached at bootFromPersistence or only on Dart start? If channel stays nil, buffer accumulates up to cap 50, FIFO evicts — no unbounded growth, but up to 50 place-level entries persist in UserDefaults indefinitely (no age limit), included in backups. If channel gets attached (does Dart's constructor-registered handler attach the native side? No — native channel property is set natively; need to check where native sets self.channel. Probably in an attach method called from application(didFinishLaunching) via the Flutter engine registration, or when Dart invokes any method on the channel? Native side: the FlutterMethodChannel is typically created in AppDelegate or in the coordinator's attach(engine). Let me check where native sets `channel` in SubtleWakeCoordinator.

  Actually, step back for adjudication scope. The coordinator's specific claim: "Dart's drainBufferedWakes returns early unless isSupported" — that is FALSE per :306-307 (platform-only check). But the finding's survival doesn't hinge on that precise mechanism; it hinges on whether there's any path where the buffer persists without clear. Consider the realistic case: flag ON (the feature is enabled; H-PRIV-1 is about the feature's own data hygiene). Buffer is drained and acked whenever Dart is alive and the app becomes active with channel attached. "No path ever clears it" — overstated: there IS a clear path (drain+ack), and it's reasonably reliable in the flag-on world. The failure modes: (a) flag-off/stale-state — buffer may persist but only if previously flag-on; (b) UserDefaults persists across updates and is backed up — true; (c) no age limit — true (count-only FIFO); (d) uninstall clears it.

  Hmm. So where does that leave H-PRIV-1? Original report text: "Native wake buffer persists up to 50 raw GPS fixes in UserDefaults with a count cap and no age limit, and no path ever clears it — survives updates, included in backups."

  Adjudicated truth:
  - File: SubtleWakeCoordinator.swift — the coordinator is right that I refuted the wrong buffer. I concede that.
  - Cap 50 ✓ (:22, FIFO at :310-312).
  - "raw GPS fixes" — wrong precision; SLC/CLVisit place-level coords (:407-408, :442-443, SLC-only manager :400). Coordinator already concedes this, rewriting as "place-level coordinates" ✓ correct.
  - "no age limit" ✓ (count-only eviction).
  - "no path ever clears it" — OVERSTATED. ackBuffer (:335-346) clears on drain+ack; drain runs on iOS whenever onWakeBuffered fires or start() succeeds, regardless of the Dart flag (drain doesn't check isSupported). The clear path exists and runs in the normal flag-on case. The accurate statement is: "cleared only via the Dart drain+ack path; entries that never drain (feature disabled after being enabled, or persistent pre-Dart native buffering) persist indefinitely — count-capped, no age bound, in backups."
  - Also worth noting: each entry carries lat/lon/acc/ts and region ids — place-level location history.

  So my verdict on dispute 1: agree-with-modification. The coordinator is right that the finding stands against SubtleWakeCoordinator and that my refutation hit the wrong file. But their specific supporting claim about drainBufferedWakes returning early unless isSupported is wrong — drain is flag-blind; the clear path exists whenever Dart runs on iOS and the native channel is attached. The finding as rewritten (place-level coords, count cap only, no age bound) lands correctly with one more edit: replace "no path ever clears it" with "cleared only on Dart drain+ack."

  Now dispute 2, H-RT-3. Facts established:
  - Drain preserves capture time for upload (:340-343, :2268) — I was right about that.
  - But _ingestForeignSample calls rangeEstimator.addObservation (:2191-2199) which has no timestamp parameter and stamps _now() internally (range_estimator.dart:79, 85) — so the 90-second sliding-window classifier treats a 20-minute-old drained sample as current. Confirmed.
  - _lastForeignScanAt = DateTime.now() at :2220 ignores `at`. The scan watchdog at :493-497 restarts the scanner if no foreign scan in 15 min — a drain of old buffered samples bumps _lastForeignScanAt, so the watchdog sees the scanner as healthy for another 15 min even if the live scanner is dead. Confirmed mechanism. But note the comment at :704-707 and :1908-1913: the team knows _lastForeignScanAt conflates "peer decoded" with "scanner alive" and added lastAnyResult tracking (W8) — but the watchdog at :493-497 still uses _lastForeignScanAt. Hmm, actually is the watchdog the :493 one? It restarts on 15-min silence. A native-buffer drain bumps the timestamp → masks a dead scanner for one watchdog cycle per drain. Real but bounded effect.
  - Also `classify()` at :2255 computes estimatedBand at drain time with the stale sample in the window → uploaded band and local "Close By" reflect a sample up to ~token-lifetime old as if fresh. The coordinator's claim "false 'Close By' for a peer who was near 20 minutes ago" — plausible: native buffer holds sightings while app suspended; on drain, all get stamped now; estimator window (90s) classifies the peer as near NOW; onSighting fires to local encounter store. So the local feed shows the peer as currently close. Server-side gets true timestamps, so the encounter record is fine; the false signal is local/on-device.

  So the coordinator is right on dispute 2: my refutation was wrong in conclusion. The mechanism as originally written in the report ("replayed into the live 90s window with fresh timestamps") is actually accurate at the estimator boundary — the estimator does stamp fresh timestamps. My delegate even saw this and waved it as "deliberate late-evidence design," but the late-evidence design (0053) is a server-side concept tied to true capture times; nothing in the estimator suggests deliberate now-stamping of drained samples — addObservation simply has no timestamp parameter, and the upload path's careful true-time handling (:2265-2267 comment) shows the authors cared and missed the estimator/watchdog boundary.

  Remaining nuance: is the effect as stated? "false Close By for a peer who was near 20 minutes ago" — the native buffer is an iOS thing for when the engine is dead/suspended; entries there can be up to... the buffer is capped (500-entry UserDefaults buffer per W5LinkController comment; or the W4 native buffer). Age depends on how long the app was dead — could be hours. So "20 minutes" is illustrative, fine.

  So verdict on dispute 2: I agree — withdraw my refutation. H-RT-3 stands, arguably with sharper wording: the timestamp survives the drain and is discarded at the estimator boundary (range_estimator.dart:79) and at _lastForeignScanAt (:2220); the upload path is correct. Severity: High still reasonable? The effect: false local "Close By" + watchdog masking. Not server-side corruption. High seems fine, arguably Medium-High; I'll not dispute severity.

  One more check on dispute 1 wording: the coordinator asked "Does that land correctly for you?" regarding rewriting as "place-level coordinates" and keeping the retention point. My answer: yes with the edit to the "no path ever clears it" clause, and noting their isSupported claim is wrong (the drain is flag-blind; the actual gap is that the native side only offers the drain when its channel is attached and the app becomes active — need to verify when native `channel` is set to be fair... I've spent enough; I can state it as: native only calls onWakeBuffered when channel non-nil + app active (notifyBufferReady :324-330); whether channel is attached without a Dart start I did not fully trace — flag that honestly). Actually let me quickly check where native sets channel in SubtleWakeCoordinator — one grep.

• One last check on where the native side attaches its channel, since that determines when the buffer clear path can run.

• The native channel is attached at plugin registration (`register(with:)`), which happens when the Flutter engine registers plugins — i.e., whenever the engine exists, regardless of the Dart flag. So in the flag-off world: engine starts → plugin registers → channel non-nil → appDidBecomeActive → notifyBufferReady fires if buffer non-empty → Dart's constructor-registered handler (:78) receives onWakeBuffered → drainBufferedWakes runs (flag-blind, :306-346) → entries acked and cleared (handleWake no-ops but handled++ still counts and ack fires, :328-338).

  So the buffer DOES get cleared whenever the app runs with an engine on iOS, regardless of the flag. The persistence-without-clear scenario reduces to: buffer accumulates natively (only possible if monitoring is active — requires flag-on at some point or stale wantsToRun), AND the app never again runs its engine on iOS (i.e., user stopped opening the app — at which point it's also not collecting new entries unless pre-Dart boot re-arms... bootFromPersistence re-arms monitoring, SLC events fire natively, emitWake with channel... after a relaunch-for-BLE/SLC-event the engine may or may not be started; in the pre-Dart path channel may be nil → appendBuffer only).

  So the accurate residual finding: entries appended while no engine exists (pre-Dart native wakes) sit in UserDefaults until the next engine run; count-capped at 50 FIFO, no age bound, included in backups, persists across app updates. The clear path exists and is effective on every engine run. "No path ever clears it" is false. "Survives updates, included in backups" true. Real-world exposure: up to 50 place-level (SLC ~500m-3km accuracy, CLVisit venue-level) coordinates with timestamps — a coarse location-history trail in a plaintext local store that is backed up.

  Severity: the report has it as High (privacy). Given place-level (not raw GPS), effective clear-on-next-engine-run, and 50-entry cap, I'd say Medium is more honest. But the coordinator's concession already downgrades precision; severity call — I'll offer Medium, not fight hard.

  Now write the adjudication.

  Dispute 1 (H-PRIV-1): Agree-with-modification. You were right that I refuted the wrong buffer — concede plainly. SubtleWakeCoordinator.swift:21-22 (key, cap 50), :407-408/:442-443 (lat/lon), :335-346 (ack-only removal). Your "place-level coordinates" rewrite lands. But two of your supporting claims are wrong:
  1. "Dart's drainBufferedWakes returns early unless isSupported" — false. drainBufferedWakes (:306-346) checks only platform (:307); it never checks isSupported. handleWake is flag-gated (:215) but the ack fires regardless (:338 counts every entry, even unhandled/malformed).
  2. The Dart handler is registered in the constructor (:78), not gated by the flag, and the native channel attaches at plugin registration (:59-62) — i.e., on any engine run. So the clear path runs on every cold start with an engine, flag on or off.
  So the accurate retention statement: buffer is cleared on the next engine run via drain+ack; what persists is only what accumulated while no engine was running (pre-Dart native wakes, bootFromPersistence re-arm), and it persists un-aged (count-cap only) in UserDefaults, included in backups, surviving updates. "No path ever clears it" should be struck — replace with "cleared only via the Dart drain+ack path; entries buffered while engine-less persist until the next engine run, with no age bound." And severity: with place-level coords + an effective clear path + 50 cap, Medium is the honest grade; I won't die on that hill if you keep High.

  Dispute 2 (H-RT-3): I withdraw my refutation — you are right. Verified myself: _ingestForeignSample (beacon_service.dart:2191-2199) calls rangeEstimator.addObservation, which takes no timestamp and stamps _now() internally (range_estimator.dart:79, 85); _lastForeignScanAt = DateTime.now() at :2220 ignores at. The watchdog (:493-497) keys on _lastForeignScanAt, so a drain of stale buffered samples masks a dead scanner for another 15-min cycle; and classify() (:2255) computes the band at drain time with the stale sample stamped now → false "Close By" locally and a misleading uploaded band, while the server upload correctly carries the true capture time (:2268). The mechanism "fresh timestamps at the classifier boundary" is literally what happens. My delegate saw the same lines and mis-classified it as deliberate late-evidence design; that design (0053) is server-side and anchored on true capture times — the upload path's own comment (:2265-2267) shows the authors intended true-time end to end and missed the estimator/watchdog boundary. The finding stands as written; I'd only suggest the wording cite range_estimator.dart:79 as the discard point. Severity High is fine.

  Also acknowledge their correction to my correction: in my round-1 reply I endorsed the delegate's "deliberate" framing — that was the error.

  Final verdict: CONSENSUS: AGREED WITH CORRECTIONS — with the two adjudications above folded in: H-PRIV-1 reinstated as modified (place-level, cap 50, no age bound, cleared only on drain+ack; suggest Medium but accept High), H-RT-3 reinstated as written. Everything else from round 1 stands as accepted by the coordinator.

• Both disputes adjudicated against the code — I read every cited line myself this time, including the ones my earlier pass got its conclusion from.

  ## Dispute 1 — H-PRIV-1: **agree-with-modification. You were right about the file; I refuted the wrong buffer.**

  I concede the refutation error plainly. `SubtleWakeCoordinator.swift` is a separate coordinator with a separate key, and every cited line checks out: `bufferKey = "io.inrange.subtlewake.buffer"` and `bufferCap = 50` at `:21-22`; entries carry lat/lon at `:407-408` (SLC) and `:442-443` (CLVisit); the only removal is inside `ackBuffer` at `:335-346`. Your concession on precision also lands correctly — `:400` confirms this manager runs SLC fixes only, never a continuous session, and CLVisit coordinates are venue-level, so "place-level coordinates" is the right wording, not "raw GPS."

  But two of your *supporting* claims are wrong, and they change the retention clause:

  - **"Dart's `drainBufferedWakes` returns early unless `isSupported`" — false.** I read the body: `subtle_wake_service.dart:306-346` checks only `_platform != TargetPlatform.iOS` (`:307`). It never checks `isSupported`. `handleWake` is flag-gated (`:215`), but the ack fires regardless — every entry, including unhandled and malformed ones, increments `handled` and is acked at `:328-338`. The drain is flag-blind.
  - **The clear path is not gated by the flag at either end.** The Dart handler is registered in the constructor (`:78`), and the native channel attaches at plugin registration (`SubtleWakeCoordinator.swift:59-62`) — i.e., on any engine run. So `appDidBecomeActive → notifyBufferReady → onWakeBuffered → drain → ack` clears the buffer on every cold start with an engine, flag on or off.

  So the accurate retention statement is: the buffer **is** cleared on the next engine run via drain+ack; what persists is only what accumulated while no engine existed (pre-Dart native wakes from `bootFromPersistence`), and that persists un-aged — count-capped FIFO only (`:306-314`), no age bound, in UserDefaults, included in backups, surviving updates. **"No path ever clears it" should be struck** and replaced with "cleared only via the Dart drain+ack path; entries buffered while engine-less persist until the next engine run, with no age bound."

  With place-level precision, an effective clear-on-launch path, and a 50-entry cap, I'd grade this **Medium** rather than High — that's my residual disagreement, and it's a severity preference, not a factual one. Keep High if you weight the backup-inclusive location trail more heavily; recorded either way.

  ## Dispute 2 — H-RT-3: **I withdraw my refutation. You are right.**

  Verified line by line: `_ingestForeignSample` forwards `at` to the upload path (`beacon_service.dart:2221`, true time preserved into `SightingRecord.observedAt` at `:2268`) — but at `:2191-2199` it calls `rangeEstimator.addObservation`, which takes **no timestamp parameter** and stamps `_now()` internally (`range_estimator.dart:79, 85`), and then sets `_lastForeignScanAt = DateTime.now()` at `:2220`, ignoring `at`. The scan watchdog (`:493-497`) keys on `_lastForeignScanAt`, so draining 20-minute-old buffered samples marks the scanner healthy for another 15-minute cycle; and `classify()` at `:2255` computes the band at drain time with the stale sample stamped as now — a false local "Close By" and a misleading uploaded band, while the server correctly receives the true capture time.

  So the finding's original mechanism — "replayed into the live 90s window with fresh timestamps" — is literally what the estimator does. My earlier pass saw the same lines and mislabeled it "deliberate late-evidence design"; that design (0053) is server-side and anchored on true capture times, and the upload path's own comment (`:2265-2267`) proves the authors intended true-time end to end and simply missed the estimator/watchdog boundary. I endorsed that mislabeling in round 1 — that was the error, and it's mine. The finding stands as written; the only edit I'd suggest is citing `range_estimator.dart:79` as the discard point so the fix lands in the right file (either thread `at` through `addObservation`, or drop drained samples older than the window from the estimator and the watchdog bump). Severity High is fine.

  ## Verdict

  **CONSENSUS: AGREED WITH CORRECTIONS** — with these two adjudications folded in:

  - **H-PRIV-1 reinstated as modified**: `SubtleWakeCoordinator.swift`, cap 50, place-level coordinates, count-cap-only with no age bound, backups/updates persistence — but "no path ever clears it" struck in favor of the drain+ack wording above (your `isSupported`-gating claim doesn't survive `subtle_wake_service.dart:306-346`). Severity: I say Medium, you say High — recorded disagreement, your call.
  - **H-RT-3 reinstated as written**, sharpened to name `range_estimator.dart:79` and `beacon_service.dart:2220` as the discard points.

  Everything else from round 1 stands as you've already accepted it (C-SQL-2 → High with the corrected premise, C-W5-1 mechanism rewrite, C-CONSENT-1 → High, H-PRIV-1's sibling corrections, the three added missing findings). With these two folded in, I can co-sign the report.

To resume this session: kimi -r session_7d89f4bc-8080-4bd9-aaf2-66369260923e
