You are running an INDEPENDENT adversarial security audit of the In Range codebase (BLE proximity social/dating app). You have audited this codebase before (the W5 encounter-lease rounds 7 and 8 on PR #9) — this is a NEW, broader round. Review only: do not edit, commit, or deploy anything.

Repo: /home/hazypiff/in-range (branch main).
A worktree of the W5 feature branch `fix/w5-encounter-lease` is at:
/tmp/claude-1000/-home-hazypiff/1e3a1f4f-6936-44cf-bd17-a5d63d0a6209/scratchpad/w5

THREAT MODEL: the attacker fully controls their own phone (rooted/jailbroken, modified app, can replay and spoof BLE frames, can run many emulated clients) and holds valid credentials, but cannot compromise the server or steal another user's auth token.

YOUR ASSIGNED ANGLE for this round — protocol and economy, end to end. Other reviewers are covering the iOS Swift internals and the Flutter runtime, so do NOT spend your effort there. Concentrate on:

1. ENCOUNTER FORGERY, end to end across the trust boundary. Trace the full chain: a token is minted server-side (issue_token_batch, migration 0031, plus 0060 batch token preclaim) -> advertised over BLE -> observed by a peer -> claimed (claim_token) -> reported (record_sighting) -> correlated into an encounter (correlate_encounter, reciprocal-confirmation gate in 0029, late-evidence tolerance in 0053). At EVERY hop ask: what binds this datum to physical proximity, and what does an attacker who controls the client need to fake to break that binding? Pay particular attention to relay attacks (attacker relays a real advertisement from location A to location B), to the width of the late-evidence window (default 15 min, clamp 2-25 — quantify what that buys an attacker), and to what the 0032 relay-abuse detection actually catches versus what it misses. The 0029 header explicitly forbids calling mutual_ble "relay-proof" — your job is to state precisely and concretely WHAT IT IS proof of, and what it is not.

2. THE POINTS ECONOMY, which is designed but NOT YET BUILT. Read docs/GAMIFICATION_SOCIAL_BLUEPRINT_2026-07-31_JOINT.md sections 2, 3 (especially 3.3) and 4, plus the X1 through X9 exploit countermeasure list. The plan is to mint points inside the correlate_encounter transaction with a ledger UNIQUE(pair_key, day, kind) and pair decay on canonical (least, greatest) user ids. Evaluate that design adversarially BEFORE it is built: double-mint under concurrency and retries, transaction isolation and the check-then-insert race, day boundary and timezone abuse, pair_key canonicalization bugs, decay reset abuse, and whether each of X1 through X9 is actually achievable against the schema that exists today. Name any countermeasure that cannot be implemented as described and say what would be needed instead.

3. PRIVILEGE AND ORACLE SURFACE across ALL migrations, with emphasis on the newest ones (0056 calibration_rssi_samples, 0057 subtle_wake_support, 0058 subtle_wake_privacy, 0059 proximity_wake_producer, 0060 batch_token_preclaim, 0061 close_wake_batch_privilege_hole, 0062 waitlist_zone). Prior rounds found two instances of one specific bug class: a SECURITY DEFINER function usable as a cross-user oracle (consent_withdrawn), and functions anon-executable because migrations granted `authenticated` but never REVOKEd the inherited PUBLIC grant (lookup_claim, bump_encounter_pair). Systematically check whether that class recurs in the newer migrations. Also check RLS policies on every table added since 0029 for USING(true), for missing WITH CHECK, and for policies that check user_id without verifying ownership of a referenced row.

CRITICAL METHOD NOTE: migrations are cumulative and functions get redefined by later migrations. Before reporting anything about a function, find its LATEST definition and review that one. Verifying this every time is mandatory — it is the most common way this kind of review produces false findings.

There is a local Postgres container `supabase_db_in-range` on port 54322 which may be running. If it is, you may query it READ-ONLY to check grants, policies, and function bodies. Never touch production.

OUTPUT: a findings report, most severe first (Critical / High / Medium / Low). For each finding give: the file and line, the concrete exploit steps from the attacker's position, whether it is gated by one of the rollout flags (enforce_consent, enforce_batch_tokens, require_attestation are all currently 0/OFF), the root cause, a specific suggested fix, and your confidence (CERTAIN if you traced it in code, LIKELY, or SPECULATIVE). Try to refute each of your own findings before including it, and drop the ones you refute. A short list of real findings is far more valuable than a long list of speculation. End with a section stating plainly what mutual_ble IS and IS NOT proof of, in language we can paste into a design doc.
