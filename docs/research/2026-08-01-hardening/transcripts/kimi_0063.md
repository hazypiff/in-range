• Let me read the migration file, then diff the three function bodies against the originals (0060, 0053, 0059), and check sightings table constraints and write paths for question (a).

  Key checks:
  a) sightings.observer_lat/lon NULL constraints — check CREATE TABLE sightings and the write paths. record_sighting requires p_lat/p_lon? In 0053 record_sighting — let me check whether it validates lat/lon non-null. Also native-buffered uploads: _flushSightings skips records lacking lat/lon (beacon_service.dart:2459 per agent 4). But are there other writers of sightings? Also the reverse sighting in correlate: the reciprocity SELECT fetches reverse sighting's observer coords — when would those be NULL? If sightings table allows NULL observer_lat, and some path writes NULL... Check the schema.

  Also important: the fallback compares "the caller's coordinates" (p_lat/p_lon passed to correlate_encounter from record_sighting — which are the observer's coords of the FORWARD sighting) against reverse sighting's observer coords. But wait — is p_lat/p_lon guaranteed non-null when correlate is called? record_sighting passes p_lat, p_lon — are those validated NOT NULL in record_sighting? In 0053 record_sighting body — need to check. If record_sighting requires lat/lon, forward side is fine. The reverse sighting's observer coords: the reverse row was also written via record_sighting, so if record_sighting always requires coords, both are non-null and fail-closed never triggers in practice. But: the fallback fails closed if either is missing — "silently kills real encounters" only if a legit path writes NULLs. Check: sightings table definition (0001?), and does record_sighting reject NULL lat/lon? From my earlier read of 0053:88-89 "only validates ranges" per agent — need to check whether NULL is allowed. Agent 1 (economy) said "identical fabricated GPS (0053:88-89 only validates ranges — coords are caller-controlled)". If p_lat can be NULL in record_sighting, then forward sightings with NULL coords exist... but then correlate's veto guard `p_lat IS NOT NULL` — the original veto only ran when p_lat non-null AND claim non-null. With the fallback, if p_lat is NULL, fail-closed RETURN kills the encounter — behavior change for clients that don't send coords. Does the real client ever send NULL? beacon_service skips upload of coord-less records. So sightings written by honest clients always have coords. But wait — early-session sightings with no fix: "_flushSightings skips those records (they fail record_sighting's required lat/lon)" — agent 4's note says record_sighting REQUIRES lat/lon. Let me verify in 0053 record_sighting: probably `IF p_lat IS NULL OR p_lon IS NULL THEN RAISE`. If so, sightings.observer_lat/lon are always non-null in practice (assuming the column allows NULL but the RPC enforces). Also check the table constraint.

  b) GREATEST on valid_until: can the token's own owner exploit ever-growing window? The owner calls claim_token with p_valid_until ≤ now+21min (0060:114-116 check on EXCLUDED value — the new value is still clamped to 21 min). GREATEST(existing, excluded) — existing was itself ≤ its claim time +21min. Each successive claim can extend by up to 21 min from each new claim time... but claim rate limit is 1 per 5s. So owner could keep a token resolvable indefinitely by re-claiming every minute with valid_until = now+21min? Wait — but claim_token also updates token_claims (the current-claim table, ON CONFLICT (user_id)) and the history row keyed by token. Normally a user rotates tokens every 15 min; re-claiming the SAME token repeatedly with growing valid_until: the 21-min cap applies to p_valid_until relative to now at each call, so the owner can keep the token alive indefinitely by re-claiming it forever (valid_until slides forward). Before this change, the same was possible: plain assignment `valid_until = EXCLUDED.valid_until` also lets the owner re-claim with a fresh now+21min. So GREATEST adds nothing new for the owner — sliding was already possible via plain assignment. GREATEST only prevents shortening (by attacker or by owner). So no new problem: the owner's ability to extend existed before; GREATEST only stops the cross-user/attacker shortening (and owner's own accidental shortening). Actually wait — does the owner gain anything vs before? Before: attacker could shorten victim's window (DoS). Now: nobody can shorten. Owner's extension ability unchanged. Also does record_sighting's resolution rely on valid_until? Yes — token resolvable while valid_until > now - W. An owner keeping their token alive indefinitely just makes their own token resolvable longer — that's already the case for active clients that re-claim. Marginal. Answer: right, no new exploit; but note the interaction: claim_token_batch preclaims with some valid_until; later single claim with GREATEST keeps the batch's possibly-later value — fine.

  One more subtlety: GREATEST with NULLs — GREATEST in Postgres ignores NULLs? No! In Postgres, GREATEST returns NULL only if all args are NULL; it ignores NULL arguments otherwise. Wait, actually: PostgreSQL's GREATEST/LEAST ignore NULL values (return NULL only if all inputs NULL). Yes, PG differs from other DBs: NULLs are ignored. So GREATEST(existing.valid_until, EXCLUDED.valid_until) with existing NULL returns excluded. Fine.

  c) WHERE on DO UPDATE silently swallows foreign conflict — should it RAISE? The function already did token_claims upsert (ON CONFLICT (user_id)) BEFORE the history insert — that always succeeds. If history DO UPDATE no-ops on foreign row, attacker gets success. Raising would leak "token belongs to someone else" oracle — but the attacker already knows (they sniffed it from someone else). Who's harmed by the no-op? The legit owner when they later claim: their claim inserts or updates their own row... wait, if attacker claimed first (INSERT succeeded — new row owned by attacker), then the victim's own claim_token for their own token hits ON CONFLICT (token) with row owned by attacker → WHERE user_id = v_uid fails → no-op → victim's claim is silently swallowed! That's bad: the victim's real coordinates never land, and the attacker's hijacked row persists. Hmm, but this is the squat scenario (flag off, attacker claims before victim). With 0060 preclaim, victim pre-claims at batch fetch before broadcast, so attacker can never INSERT first for updated clients. For old clients, squat still possible, and now the victim can't reclaim their row via claim_token. Before the fix, victim's claim would overwrite the attacker's row (COALESCE fills, user_id stays attacker though — user_id was never updated by DO UPDATE... wait original DO UPDATE didn't set user_id, so victim's claim would update coords but row stays owned by attacker — victim never got ownership back anyway). So even before, a squatted row kept attacker as user_id. The WHERE no-op doesn't make squat recovery worse in ownership terms; it does stop the victim from geo-filling. Edge case, flag-off only.

  Should it RAISE? An oracle "this token is claimed by someone else" — the attacker gains little (they can observe this over BLE anyway). But raising on the victim's legitimate claim path would break the victim's client with an error it can't handle (it just claims tokens from its own batch; if somehow conflict... with preclaim, the row is already theirs, WHERE passes, no error). Actually when does a legit user's claim hit a foreign row? Only if squatted (old client) — rare, flag-off. RAISE vs silent no-op: silent no-op means the victim's client thinks it claimed but the row points to attacker — encounters get attributed wrong silently. RAISE would surface the anomaly. But RAISE leaks the oracle to attackers probing random tokens: "22023 token claimed by another user" tells them the token is live and owned — they already know if sniffed. For random 32-hex probing, 122-bit space makes hits infeasible. I lean: no-op is acceptable, RAISE optional; but the success-response consideration: does the attacker gain by getting success? They get success either way (before the fix too — they got success and the overwrite!). The no-op is strictly better. The real residual: victim reclaim silently swallowed — worth flagging as a known residual, maybe suggest the INSERT...ON CONFLICT path could instead detect foreign-owned existing row and log/skip; a RAISE with the SAME error as invalid token ('Invalid beacon token' 22023) would avoid a distinct oracle but break victim clients. Recommendation: keep no-op; optionally add a warning log. Answer c accordingly: no-op is the right call; raising creates a distinguishing oracle only if a distinct error code/message is used; using the same 'Invalid beacon token' error would be oracle-free but would also fire on the victim's reclaim path, breaking honest old clients during the flag-off window — so no-op is the safer choice.

  d) Diff the three bodies against originals. I'll extract and compare.

  e) BEGIN/COMMIT single transaction: CREATE OR REPLACE FUNCTION takes locks; on a live DB, replacing claim_token/correlate_encounter/cleanup_ephemeral_data requires ACCESS EXCLUSIVE on the function (brief). Risk: lock queue behind long-running calls; functions are called frequently (record_sighting 120/min). Brief blockage possible but standard. Also CREATE OR REPLACE preserves grants — verify the migration doesn't re-grant anything wrong or change volatility/search_path. Also check it doesn't DROP anything. Also the DELETE in cleanup uses existing index. Also: does 0063 update the security_regression test? Not needed.

  Also check: does the C-SQL-4 fallback compare forward caller coords (p_lat/p_lon) vs reverse observer coords — semantics: forward observer is the caller (v_uid), reverse observer is the other user. Comparing the two observers' locations — that's the observer-vs-observer veto I recommended. But careful: the original veto compared forward observer vs claim (owner's claimed location). Fallback compares forward observer vs reverse observer. Both sightings carry observer coords — good. But what radius? Uses v_radius computed in record_sighting (passed as p_radius_meters) — check the migration uses LEAST(400, GREATEST(5, p_radius_meters)) same as the main veto.

  Another subtle issue: v_veto_ran must be set correctly — the veto also requires p_lat IS NOT NULL; if p_lat is NULL (can correlate be called with NULL? record_sighting raises if p_lat NULL? need to check 0053 record_sighting validation lines 62-100) — if record_sighting requires coords, p_lat never NULL in practice.

  Wait, agent 4 said "_flushSightings skips those records (they fail record_sighting's required lat/lon)" — suggests record_sighting requires lat/lon. Verify in 0053 lines 62-108. Let me read.

  Also check sightings table: does any OTHER path insert sightings with NULL coords? grep INSERT INTO public.sightings across migrations. batch paths? 0059 proximity wake? Let me check.

  Plan of reads:
  1. Read 0063 fully.
  2. Read 0053:62-140 (record_sighting validation + call) — already have 108-140; need 62-107.
  3. Grep sightings CREATE TABLE for observer_lat constraint; grep all INSERT INTO public.sightings.
  4. Diff claim_token 0063 vs 0060; correlate_encounter 0063 vs 0053; cleanup 0063 vs 0059. Do manually by reading 0063 and comparing to what I've read (I have 0060:85-161, 0053:160-217 + 108-140, and cleanup 0059:477-580 not yet read by me — agent read it. Need to read 0059 cleanup section for diff).

  Let me start by reading 0063.

• Reading the migration first, then I'll diff each body against its original and check the sightings write paths.

• Now I need to verify several things:

  **d) Diff against originals.** 

  claim_token: I have 0060:85-161 from earlier. Compare:
  - 0060:89 signature identical.
  - Body lines 96-133 (0060) vs 0063:30-67 — identical.
  - token_claims upsert identical.
  - token_claim_history: original had valid_until = EXCLUDED.valid_until, approx_lat = COALESCE(EXCLUDED.approx_lat, ...), approx_lon COALESCE, range_type COALESCE, accuracy_m COALESCE. New: valid_until = GREATEST(existing, EXCLUDED), plain assignments, plus WHERE. Matches the described changes. One subtlety: GREATEST returns NULL if both args NULL; existing.valid_until could be NULL? Column probably NOT NULL; even if NULL, GREATEST(NULL, x) = x in Postgres (ignores NULLs). Fine.
  - Note the comment in 0063 says "could extend valid_until indefinitely" — wait, original code allowed attacker to overwrite valid_until with their own value (up to now+21min per call), so extension was possible before too; the comment's claim "and could extend valid_until indefinitely" is about the attacker... attacker could already do that with plain assignment (repeated calls every 5s). GREATEST doesn't make it worse. OK.

  correlate_encounter: original 0053:147-217. Compare against what I read earlier:
  - Signature: 0053 used `SET search_path = pg_catalog, public`? My earlier read of 0053:160-217 showed `$$;` at end; the header at 0053:147-152 — agent 1 said "0053:147-152". 0063:111 says `SET search_path = pg_catalog, public`. Need to verify 0053's original had same. Let me check 0053:147-160.
  - Declarations match plus new vars.
  - v_now := NOW() — matches 0053? Earlier read line 159-161 showed `v_late INTERVAL := ...`. I have 0053:160-217; the DECLARE block started before :160. Need 0053:147-160 to diff. Also the cleanup body diff: need 0059:477-580.

  **a) sightings.observer_lat NOT NULL?** 0063 comment claims "which are NOT NULL on every sightings row". Need to check the sightings table definition. Let me grep CREATE TABLE sightings. Also check all INSERT INTO public.sightings paths. record_sighting validates p_lat? From my 0053 read at 108-140 I saw no lat/lon validation before the INSERT — the validation might be at lines 62-107 which I haven't fully read. Agent 1 said coords "only validates ranges (0053:88-89)". If record_sighting does NOT reject NULL p_lat, then sightings rows CAN have NULL observer_lat! Agent 4 said flush skips records lacking lat/lon claiming "they fail record_sighting's required lat/lon" — that suggests record_sighting raises on NULL. Need to read 0053:62-107 to settle. Also check the sightings table constraint — if column is NOT NULL, then DB-level guarantee. Let me check.

  Also — the reverse sighting lookup: `rs.observer_user_id = v_claim.user_id AND rs.observed_user_id = v_uid` — the reverse row is the other user's sighting of the CALLER. Written by the other user's client via record_sighting. If record_sighting requires lat/lon, non-null guaranteed. But also: are there other writers of sightings? grep.

  Wait, actually important: even if record_sighting requires coords, what about accuracy? The fallback compares observer positions with radius LEAST(400, GREATEST(5, p_radius_meters)) where p_radius_meters = v_radius computed in record_sighting via gps_veto_radius_meters(p_accuracy, v_claim_acc) for feet ranges. For the fallback, the accuracy of the REVERSE sighting isn't considered — v_radius accounts for forward accuracy + claim accuracy, not reverse observer accuracy. Minor: reverse observer's accuracy could be 10km (p_accuracy up to 10000m allowed), making the comparison meaningless-ish but in the failing direction? If reverse observer accuracy is huge, the true distance could be large even if point distance is small → false negatives for legit users with poor GPS on the reverse side. The original veto had the same weakness (claim accuracy folded via gps_veto_radius_meters — actually v_radius = gps_veto_radius_meters(p_accuracy, v_claim_acc) includes claim acc; the fallback reuses the same radius, which includes claim acc even though claim has no coords... when claim is batch-preclaimed, v_claim_acc = accuracy_m from batch preclaim — is that NULL? claim_token_batch inserts accuracy? Probably NULL → gps_veto_radius_meters(p_accuracy, NULL) handles NULL presumably). This is a precision quibble, not a breaker. The 400m cap bounds it.

  Real question (a): can legit sightings rows have NULL observer coords? Check:
  1. sightings table DDL.
  2. record_sighting validation of p_lat/p_lon.
  3. Any other INSERT INTO sightings.

  From agent 4 (Dart): "_flushSightings skips those records (they fail record_sighting's required lat/lon)" — the comment in beacon_service.dart:2287-2289 says "record_sighting's required lat/lon". So record_sighting DOES require lat/lon. Verify in 0053:62-107.

  **b) GREATEST** — as analyzed: owner could already slide valid_until forward via repeated claims (plain assignment, 5s rate limit, 21-min cap per call). GREATEST adds no new capability for owner; prevents shortening by anyone. One more check: could an attacker EXTEND the victim's window? Before: yes, by overwriting with now+21min repeatedly — keeping a sniffed token resolvable indefinitely (replay window extension!). After: GREATEST prevents... no wait, attacker can't update the row at all now (WHERE user_id = v_uid). Before the fix the attacker could keep extending; that hole is closed by the WHERE. GREATEST vs plain assignment for the OWNER: owner sliding forward is same as before. So (b): GREATEST is right, no new problem. But one edge: GREATEST(existing, excluded) — existing valid_until from batch preclaim might be LATER than what a single claim would set; keeping the later one is intended. Also: does anything rely on valid_until being able to shrink? release_token? Rotation? token_claims (current) still gets plain overwrite — only history is GREATEST. OK.

  Also subtle: `valid_until = GREATEST(...)` but the INSERT also sets valid_from = v_now on new rows; on conflict, valid_from NOT updated — the ORDER BY valid_from DESC in correlate picks latest claim row per token... only one row per token (PK token). Fine.

  **c) WHERE swallows foreign conflict silently** — analysis as above. Also: claim_token's token_claims upsert (user-scoped) still happens — so the attacker's own current-claim row now points at a token they don't own in history... consequence: the attacker's client would advertise... whatever; harmless. The bigger residual: the victim's reclaim after squat is silently swallowed — but note with WHERE, the victim ALSO can't fix a squatted row; before, victim could at least fill coords (ownership stayed attacker). Actually wait — can the squatted row ever be fixed? Cleanup purges token_claim_history after valid_until + 24h; the squatted row's valid_until ≤ claim+21min, so it expires within a day. And enforce_batch_tokens flip kills the whole class. So residual is bounded. Answer: no-op is right; RAISE with distinct error = oracle (minor, token-space unguessable so only confirms what a sniffer knows); RAISE with identical error breaks honest victim reclaim; also a RAISE would abort the whole transaction including the token_claims upsert — changing behavior. Recommend keeping no-op. Also note: the batch consumption UPDATE (beacon_token_batch consumed_at) already happened before — that's owner-scoped anyway (WHERE user_id = v_uid), fine.

  **e) Transactional**: single BEGIN/COMMIT, three CREATE OR REPLACE. On live DB, each takes a brief lock on the function (pg_proc row update + invalidation); concurrent callers queue briefly. CREATE OR REPLACE preserves existing ACLs — good (no grant drift). No table DDL, no long locks, no rewrites. The DELETE in cleanup runs at next scheduled call, not in the migration. Safe. One consideration: if production is at 0055 (per 0061 header note "prod is at 0055; 0056-0060 undeployed")! Wait — 0061's header said prod was at 0055 as of that commit. Has 0056-0062 been deployed since? The rehearsal applied 0020..0063 locally. If prod is still at 0055, deploying 0063 requires 0056-0062 to deploy first/together — flag as a deployment-order question, not a defect. The audit said proximity-wake 404s (0059 undeployed) — consistent with prod behind. Worth noting: verify prod migration version before pushing 0063, since 0063 depends on 0059/0060 function shapes (claim_token from 0060, cleanup from 0059). If prod's claim_token is the pre-0060 version, applying 0063 after the chain is fine, but if someone cherry-picks 0063 onto a prod at 0055, it would REVERT claim_token's 0060 changes? No — 0063's claim_token was extracted from 0060, so it includes the 0060 changes (batch consumption etc.). But cleanup from 0059 references tables (rssi_samples, venue_anchors, proximity_wake_requests, rssi_batch_rate) that don't exist at 0055 → migration would fail cleanly rather than corrupt. Supabase applies migrations in order anyway, so 0063 can't be applied without 0056-0062. Non-issue but worth a note.

  Now remaining verifications:
  1. Read 0053:62-107 (record_sighting validation) — does it require p_lat/p_lon?
  2. sightings table DDL — observer_lat NOT NULL?
  3. Other INSERT INTO public.sightings paths.
  4. 0053:147-160 header of correlate_encounter to diff (search_path line etc.).
  5. 0059:477-580 cleanup original to diff.
  6. claim_token 0060:1-84 (the part I haven't read: claim_token_batch) — not needed for diff of claim_token; I have 85-161. Good.

  Also check: does the 0063 correlate DECLARE include v_now := NOW() — original had it? And check `SET search_path = pg_catalog, public` matches 0053 original (my earlier read of 0053:160-217 didn't show the header). Let me read 0053:62-160.Key finding for (a): record_sighting (0053:88) RAISEs on NULL p_lat/p_lon — so every row written via record_sighting has non-null observer coords. But all those INSERTs into sightings from other migrations are earlier definitions of record_sighting (superseded) — except 0008:402 and 0001:298 which might be different functions (0008:402 could be inside correlate_miles_encounters or an older record_sighting). Need to check 0008:402 context — what function is it in, and is it still live? 0008's record_location_ping/correlate_miles? Let me check. Also check the sightings table DDL at 0001:134 for NOT NULL on observer_lat.

  Also: do later migrations ALTER sightings columns? grep ALTER TABLE ... sightings.

  Let me check 0001:134-150 (sightings DDL), 0008:395-410 context, and any ALTER TABLE sightings.- sightings DDL: `observer_lat DOUBLE PRECISION NOT NULL`, `observer_lon NOT NULL` (0001:140-141). Check the two ALTERs (0029:21, 0023:23) don't drop the NOT NULL — they likely add columns (observed_user_id, observer_accuracy_m, received_at). Quick check to be thorough.
  - 0008:402 is an old record_sighting definition, superseded by later ones (0011, 0019, ..., 0053). All the INSERTs listed are inside successive record_sighting definitions. The live one (0053) requires coords. So the "NOT NULL on every sightings row" claim holds both at the DDL level and the write-path level. 

  So answer (a): the fail-closed branch cannot fire on legitimate data — observer_lat/lon are NOT NULL at the schema level (0001:140-141), and the only live writer (record_sighting, 0053:88) rejects NULL coords before insert. The Dart flush skips coord-less records precisely because the RPC rejects them. The locked-phone path is unaffected: the reverse sighting always carries the locked phone's real fix from its wake-burst flush. One residual: rows written before... no, NOT NULL always existed. Also verify the two ALTERs quickly.

  Also need to diff cleanup body vs 0059:477-580. I haven't read 0059's cleanup. Let me read it. And check correlate diff is fully verbatim — I compared against my earlier read of 0053:147-217; the 0063 body matches except marked sites: v_rev_lat/v_rev_lon/v_veto_ran declarations (:121-123), v_veto_ran := TRUE (:144), reciprocity SELECT fetches observer coords (:152-153), and the fallback block (:160-174). The header comment "Reciprocity gate..." matches. Signature matches 0053:147-152 including `SET search_path = pg_catalog, public`. Good.

  One more check on the fallback logic: v_veto_ran is set TRUE only inside the IF that requires p_lat, p_lon, claim coords all non-null. record_sighting always passes non-null p_lat/p_lon (validated at :88), so in practice v_veto_ran = TRUE iff claim has coords. Fallback runs when claim is NULL-coord (batch preclaim) — compares forward observer coords vs reverse observer coords. Correct semantics (observer-vs-observer, as recommended). Fail-closed if p_lat/p_lon NULL — can't happen via record_sighting; correlate is not callable by authenticated directly (internal only). batch_correlate_recent_pings? Does anything else call correlate_encounter? grep PERFORM/SELECT correlate_encounter. 0019:979 PERFORM public.correlate_encounter — inside what function? That's an old record_sighting definition in 0019, superseded. batch_correlate_recent_pings calls correlate_miles_encounters probably. Let me grep callers of correlate_encounter to make sure no other live caller passes NULL coords.

  Also confirm cleanup diff. Read 0059:477-580.Findings so far:

  1. **Cleanup diff**: comparing 0059:477-580 vs 0063:200-311 — bodies identical except the added beacon_token_batch DELETE at 0063:281 (with comment at :275-280). Confirmed verbatim otherwise. One difference: 0059 ends with `COMMENT ON FUNCTION public.cleanup_ephemeral_data IS 'Sweeps ephemeral tables: ...'` (:582-583) — 0063 does NOT include the comment, and CREATE OR REPLACE preserves existing comments, so the comment survives but is now stale (doesn't mention beacon_token_batch). Minor: comment on function now incomplete — cosmetic, worth one line.

  Also note: the new DELETE at 0063:281 is placed AFTER the legal-hold IF/ELSE block, so it runs regardless of holds — and it does NOT check has_legal_hold. Is that a problem? Under a legal hold, token_claim_history is preserved for held users; beacon_token_batch maps the same tokens to user_ids. If the point of the hold is preserving evidence, purging beacon_token_batch under a hold could destroy hold-relevant linkage. Conversely, rssi_samples correlation joins... Legal hold semantics: the sightings/history are preserved; deleting the batch token map under a hold undermines the hold's completeness (the batch table is arguably part of the same evidence chain — 0044 evidence preservation). Hmm — but is beacon_token_batch in the legal-hold preservation design? The account-deletion scrub paths delete it even for... 0035/0037/0044 delete it on account deletion/scrub — actually 0037:172 is legal_hold migration deleting it? Agent 5 said account-deletion/scrub paths include 0037:172 (legal hold migration!) and 0044:204 (evidence preservation). So the hold architecture itself treats beacon_token_batch as deletable PII, not evidence to preserve. Interesting — 0044:204 is in evidence_preservation and deletes beacon_token_batch (probably in scrub_account_pii). So the hold system deliberately scrubs it as PII. Then a hold-blind purge is consistent with existing treatment. But strictly, under an active hold the cleanup preserves token_claim_history for held users while deleting their beacon_token_batch rows — mild inconsistency but aligned with how 0044 treats the table (PII to scrub, not evidence). I'll note it as a consideration, not a blocker: optionally add `AND NOT public.has_legal_hold(user_id)` inside the v_holds branch for consistency... but the DELETE is outside the IF. The other tables have hold-aware variants. For strict consistency, the batch purge could be hold-aware. But given 0044 scrubs it during evidence preservation flows, hold-blind is defensible. Flag as a question with a recommendation.

  Also: index for the DELETE — idx_beacon_token_batch_expiry exists (0031:39). Good.

  2. **Correlate diff**: 0063:106-198 vs 0053:147-217 — identical except marked sites. Verified: signature identical including `SET search_path = pg_catalog, public`; declarations identical + 3 new vars; v_veto_ran := TRUE added; reciprocity SELECT widened; fallback block added; rest byte-identical. Callers: only record_sighting (0053:137, latest def) calls it; other PERFORMs are in superseded record_sighting definitions. record_sighting validates coords (0053:88) so p_lat/p_lon never NULL in practice. batch callers? None live.

  3. **claim_token diff**: verified against 0060:89-161 — identical except the marked history-upsert changes. 

  4. **(a)** — sightings.observer_lat/lon NOT NULL (0001:140-141); live writer rejects NULL (0053:88); Dart flush skips coord-less records. Fail-closed branch is unreachable on real data — it's pure defense in depth. The locked-phone path is unaffected: the reverse sighting comes from the locked phone's wake flush with its real fix. One caveat: the reverse sighting lookup picks the most recent reverse row within W — with real coords. Also the radius: v_radius = gps_veto_radius_meters(p_accuracy, v_claim_acc) — when the claim is batch-preclaimed, v_claim_acc comes from token_claim_history.accuracy_m which claim_token_batch may leave NULL; gps_veto_radius_meters presumably handles NULL claim acc (need to check? It's existing behavior for the radius computation — unchanged by 0063; the fallback reuses the same radius. The reverse observer's accuracy is NOT folded into the radius — a reverse fix with 10km accuracy could produce false vetoes (distance measured point-to-point up to 400m cap; if reverse observer's fix is off by 2km, a legitimate co-located pair fails). Original veto had claim accuracy folded in via gps_veto_radius_meters; fallback has no accuracy term for the reverse side. Real-world: iOS wake-burst fixes can be coarse (SLC-derived, hundreds of m to km). This could cause false negatives — legitimate encounters vetoed — for exactly the locked-phone population. Radius cap 400m + typical SLC accuracy ~ hundreds of m → borderline. Worth flagging as a tuning concern: consider folding rs.observer_accuracy_m into the fallback radius (e.g., gps_veto_radius_meters(p_accuracy, rs.observer_accuracy_m)). That's a legitimate improvement suggestion — accuracy is stored on the sightings row (observer_accuracy_m). I'd rate this a "should fix" for the locked-phone path — otherwise C-SQL-4's fix may silently kill real encounters in poor-GPS conditions, which is question (a)'s real teeth. Hmm, but the 400m cap... in dense urban use 400m is the design bound anyway; the accuracy-aware gps_veto_radius_meters exists precisely because fixes lie. For an honest recommendation: use gps_veto_radius_meters(p_accuracy, v_rev_acc) capped at 400. SAFE WITH CHANGES? Let me think about severity: false veto = missed encounter (product degradation), not a security hole. The migration improves security without creating a new exploit. The accuracy concern is a product-quality tuning issue. I'd call it SAFE WITH CHANGES (recommend folding reverse accuracy; not a hard blocker but cheap and directly responsive to (a)).

  Wait, also check: does the SELECT into v_rev_lat/v_rev_lon pick the reverse sighting with coords consistent with time? ORDER BY received_at DESC — most recent. Fine.

  Another check on the fallback: `IF NOT v_veto_ran THEN IF p_lat IS NULL...` — p_lat NULL can't happen via record_sighting; direct calls impossible (no authenticated grant). Fine.

  5. **(b) GREATEST** — owner could already extend indefinitely via repeated claims (plain assignment, 5s rate limit, 21-min cap each). GREATEST only removes the ability to shorten. No new owner exploit. Also Postgres GREATEST ignores NULLs — safe if existing.valid_until is NULL (column likely NOT NULL anyway). One more angle: does the ever-growing history valid_until interact with the claim floor at correlate (:130) — valid_from > NOW() - max(23+late, 30) — a token re-claimed repeatedly keeps valid_from static (not updated on conflict) — the row's valid_from stays the original claim time; after ~53 min the floor excludes it regardless of valid_until. So indefinite extension via re-claim doesn't actually keep it resolvable forever through claim_token history row — the floor at :130 kills it after 25+late minutes from FIRST claim. Wait — but each new claim... the row is ONE row per token (PK token); ON CONFLICT updates it; valid_from never changes on update. So the correlation floor bounds the token's life to ~25+W min from first claim regardless of GREATEST. Even better — extension beyond that was already impossible for correlation purposes (though record_sighting's resolution at :102-105 has no valid_from floor — it resolves while valid_until > now - W. So owner re-claiming extends record_sighting resolution indefinitely... same before and after; plain assignment allowed it too). Conclusion: GREATEST introduces no new capability; answer (b) = it's right, with the note that extension-by-owner already existed and is bounded for correlation by the valid_from floor.

  Actually wait — one NEW edge from GREATEST: an ATTACKER with a sniffed token can no longer update the row at all (WHERE clause), so no. And the owner shortening? Non-issue.

  6. **(c)** — no-op is the right choice. Analysis: RAISE with a distinct message = oracle (confirms token live+owned — but a sniffer already knows; random probing is 122-bit infeasible; low value either way). RAISE even with identical error aborts the txn and would also fire on the victim's own legitimate reclaim after a squat (old-client flag-off path), breaking honest clients — the no-op keeps victim claims succeeding (their token_claims row updates; history stays squatted until it expires ≤24h via cleanup... wait history purge is valid_until < now-24h, squatted row's valid_until ≤ +21min so it's purged within a day; also resolution ORDER BY valid_from — the squatted row with attacker user_id... victim's subsequent tokens are new rows, fine). Residual: during the flag-off window, a successful squat still misattributes that token's sightings to the attacker for the token's ~1-day history life (bounded by record_sighting resolution: valid_until > now-W — only ~21min+W actually resolvable). Acceptable; the report already recommends the flag flip. Also note claim_token's INSERT ... ON CONFLICT for a never-claimed token still lets the attacker INSERT (user_id=attacker) — the WHERE only guards the UPDATE path. That's the remaining squat vector, flag-gated (enforce_batch_tokens=0). The migration doesn't make it worse; worth one line that the full closure is the flag flip, as the audit said.

  7. **(e) transactional** — single transaction, three CREATE OR REPLACE: brief DDL locks on pg_proc rows; concurrent record_sighting/claim_token calls queue for the duration of the migration (milliseconds). CREATE OR REPLACE preserves ACLs and comments — no grant drift (0061's invariant intact; the security_regression.sql assertion still passes since grants unchanged). No table rewrites, no long-held table locks. The new cleanup DELETE uses idx_beacon_token_batch_expiry. Deployment-order note: prod chain — if prod is behind 0062 (audit suggested 0059 undeployed; 0061 header said prod at 0055 at that time), 0063 applies only after 0056-0062; rehearsal covered the chain. Also: 0063 header says "local container is at 0019, could not be executed end-to-end during authoring" but the user says rehearsal applied 0020..0063 on a full-scaffold DB — fine.

  Also confirm the COMMENT ON FUNCTION for cleanup — 0063 doesn't restate it; preserved but stale (doesn't list beacon_token_batch). Suggest adding an updated COMMENT (optional).

  Also claim_token comment in 0063 says "could extend valid_until indefinitely" — slightly off: GREATEST vs the old plain assignment — the OLD code let anyone (incl. attacker) set valid_until to their caller value each call, i.e., extend; the comment attributes that to pre-fix state, which is accurate (attacker could extend before; now they can't). OK.

  One more possible accidental-diff check: 0063 correlate uses `AS $$` and `SET search_path = pg_catalog, public` — matches 0053 (:152-153). claim_token uses `SET search_path TO 'pg_catalog', 'public'` — matches 0060:93. cleanup `SET search_path = pg_catalog, public` — matches 0059:481. Volatility: none specified anywhere (default VOLATILE) — matches originals.

  GRANT check: does 0063 contain any GRANT/REVOKE? No — CREATE OR REPLACE preserves ACLs. Good — 0061's invariant intact.

  Also check 0023/0029 ALTERs on sightings don't relax NOT NULL — 0023:23 and 0029:21 add columns presumably (observer_accuracy_m, received_at, observed_user_id). The NOT NULL on lat/lon from 0001 stands; I could verify quickly but ALTERs that DROP NOT NULL are rare; let me just check quickly to be thorough. Actually I've used a lot of calls; one more small grep for those two ALTERs' content.

  Also should I double check `gps_veto_radius_meters` handles NULL claim acc (for the fallback radius path)? v_radius is computed in record_sighting regardless of my changes — existing behavior for the claim-less case: v_claim_acc would be NULL for batch rows and gps_veto_radius_meters(p_accuracy, NULL) — presumably COALESCEs to a default. This is pre-existing, unchanged. Skip.

  Verdict: SAFE WITH CHANGES — the changes being: (1) fold reverse observer accuracy into the fallback radius (or consciously accept 400m point-to-point with a comment) — this is the real answer to (a)'s "silently kills real encounters" risk: not NULLs (impossible) but coarse reverse fixes; (2) optional: make the batch purge hold-aware or document why hold-blind is intended; (3) optional: refresh the stale COMMENT ON FUNCTION. None are security blockers. Actually should the verdict be SAFE TO DEPLOY with notes, or SAFE WITH CHANGES? The accuracy-fold is a genuine product-correctness improvement directly answering (a); I'd say SAFE WITH CHANGES — change #1 recommended before deploy, #2/#3 optional. Let me quickly check the two sightings ALTERs and gps_veto_radius_meters NULL handling to be precise.

• Two last checks: the sightings ALTERs (do they touch the NOT NULLs?) and how `gps_veto_radius_meters` handles a NULL claim accuracy, since the fallback reuses that radius.

• All checks done. Summary of analysis:

  (a) Fail-closed branch and NULLs: `sightings.observer_lat/lon` are NOT NULL at the schema level (0001:140-141); the only live writer, record_sighting (0053:88), rejects NULL coords before insert (which is exactly why the Dart flush skips coord-less records); the 0023/0029 ALTERs only ADD columns, don't relax anything. So v_rev_lat/v_rev_lon can never be NULL on any row, and p_lat/p_lon can never be NULL via the sole live caller (0053:137). The fail-closed branch is unreachable defense-in-depth — it cannot kill real encounters. The locked-phone path works: the reverse sighting is the locked phone's wake-burst flush carrying its real fix.

  BUT the real (a)-adjacent risk is accuracy, not NULLs: the fallback radius reuses v_radius = gps_veto_radius_meters(p_accuracy, v_claim_acc). For a batch-preclaimed claim, v_claim_acc (token_claim_history.accuracy_m) is likely NULL → COALESCE 100m → radius = max(100, 2*(acc_f + 100)) capped 400. The reverse observer's OWN accuracy (sightings.observer_accuracy_m, available on the row but not selected) is never folded in. A reverse fix from an SLC/wake-burst can be hundreds of meters to km off; a point-to-point comparison ≤400m against a stale/coarse reverse fix will veto some genuine co-located encounters. Recommendation: select rs.observer_accuracy_m too and compute the fallback radius as gps_veto_radius_meters(p_accuracy, v_rev_acc) (still capped at 400). This is a correctness/tuning improvement, not a security blocker — false vetoes degrade the product, they don't create an exploit; and the status quo ante was NO spatial check at all for these tokens, so even a coarse fallback is strictly better security-wise.

  (b) GREATEST: right; no new owner capability. Owner could already slide valid_until forward with repeated claims (5s rate limit, 21-min cap per call) under the old plain assignment. GREATEST only removes shortening. Bonus: for correlation, the valid_from floor (0053:130 in 0063 numbering — the claim floor) bounds a token's correlatable life to ~25+W min from first claim regardless, since valid_from never changes on conflict. Postgres GREATEST ignores NULLs, so a NULL existing.valid_until is safe.

  (c) No-op is the right call. RAISE with a distinct message creates a "token is live and owned" oracle (low value — 122-bit space makes blind probing infeasible, and a sniffer already knows what they sniffed — but there's no reason to hand it out). RAISE even with an identical error code aborts the transaction and, worse, fires on the victim's own legitimate reclaim after a squat during the flag-off window, breaking honest old clients. The no-op bounds the residual: a squatted row stays resolvable only while valid_until > now−W (≤ ~36 min), then expires; cleanup purges it within 24h; enforce_batch_tokens=1 closes the insert-side squat entirely. Note the WHERE only guards the UPDATE path — the INSERT path (token never claimed) still lets an attacker insert a row with user_id=attacker while the flag is off. That's the known residual the audit already assigned to the flag flip; the migration neither fixes nor worsens it. Worth one line in the header comment so the next reviewer doesn't think C-SQL-1 is fully closed pre-flag.

  (d) Diffs: all three bodies are verbatim except the marked sites — I compared all three against 0060:85-161, 0053:147-217, 0059:477-580 line by line. Two cosmetic deltas: (1) 0063 omits the `COMMENT ON FUNCTION public.cleanup_ephemeral_data` from 0059:582-583 — CREATE OR REPLACE preserves the existing comment, which is now stale (doesn't list beacon_token_batch). (2) Nothing else. Grants: no GRANT/REVOKE in 0063; CREATE OR REPLACE preserves ACLs, so 0061's privilege invariant and the security_regression.sql assertion are unaffected.

  (e) Transactional: fine. One BEGIN/COMMIT, three CREATE OR REPLACE = brief pg_proc row locks; concurrent record_sighting/claim_token calls queue for milliseconds. No table DDL, no rewrites. The new DELETE runs at the next scheduled cleanup call, not in the migration, and idx_beacon_token_batch_expiry (0031:39) covers it. Deployment-order note: 0063 depends on the 0059/0060 shapes; if prod is behind (the audit found proximity-wake 404ing, 0061's header said prod was at 0055), the chain 0056-0062 must land first or with it — your rehearsal applied 0020..0063 in sequence, which is the right shape.

  One more consideration for cleanup: the new batch DELETE is outside the legal-hold IF/ELSE, so it purges even for held users. Consistency argument: under an active hold, token_claim_history is preserved per-user but beacon_token_batch (same token→user mapping) is not. Counter-argument: 0044 (evidence preservation) itself scrubs beacon_token_batch as PII (0044:204), so the hold architecture already treats the table as scrubbable PII rather than evidence. I'd suggest either making it hold-aware in the v_holds branch for symmetry, or adding a comment stating the hold-blind purge is deliberate because 0044 treats the table as PII. Not a blocker — flag it.

  Verdict: SAFE WITH CHANGES — one recommended change (fold reverse accuracy into the fallback radius), two optional (hold-aware batch purge or a comment; refresh the stale function comment; plus header note about INSERT-side squat remaining flag-gated).

• **VERDICT: SAFE WITH CHANGES** — one recommended fix before deploy (the fallback radius ignores the reverse sighting's accuracy, which is the real answer to your question a), two optional. I diffed all three bodies line-by-line against 0060:85-161, 0053:147-217, and 0059:477-580; the "verbatim except marked sites" claim is accurate, and I verified the schema/write-path facts the changes rely on rather than trusting the comments.

  **a) The fail-closed branch cannot fire on real data — but the fallback has an accuracy blind spot.**

  The NULL fear is unfounded, at both layers: `sightings.observer_lat/lon` are `NOT NULL` in the DDL (`0001:140-141`), and the only live writer — `record_sighting` (0053:62, all the other `INSERT INTO sightings` hits are superseded definitions) — rejects NULL coords before insert (`0053:88`). That is precisely why the Dart flush drops coord-less records. The 0023/0029 ALTERs only ADD columns. And `p_lat/p_lon` reach `correlate_encounter` only from `record_sighting` (`0053:137`; every other `PERFORM correlate_encounter` is in a superseded record_sighting), so they are never NULL either. Your fail-closed `RETURN` is unreachable defense-in-depth. The locked-phone path is unaffected: the reverse sighting is the locked phone's own wake-burst flush carrying its real fix.

  The actual risk to that path is **accuracy, not NULLs** (`0063:166-173`). The fallback reuses `p_radius_meters`, which `record_sighting` computes as `gps_veto_radius_meters(p_accuracy, v_claim_acc)` (`0053:134`) — the *claim's* accuracy. For a batch-pre-claimed row that accuracy is NULL (COALESCEd to 100 m by `0023:36-38`), and the reverse sighting's **own** accuracy — `sightings.observer_accuracy_m`, sitting on the row you already SELECT from — is never folded in. An SLC/wake-burst reverse fix can be hundreds of meters off; a ≤400 m point-to-point comparison against it will veto some genuine co-located encounters in exactly the population 0060 exists to serve. **Recommended change:** also select `rs.observer_accuracy_m` and compute the fallback radius as `public.gps_veto_radius_meters(p_accuracy, v_rev_acc)` (still 400-capped). Not a security blocker — even a coarse fallback is strictly better than the veto not running — but cheap and directly responsive to your question.

  **b) `GREATEST` is right and adds nothing exploitable.** The owner could already slide `valid_until` forward indefinitely under the old plain assignment (one claim per 5 s, each ≤ now+21 min) — `GREATEST` only removes *shortening*, for everyone. For correlation specifically the `valid_from` floor (`0063:130`) bounds a token's life to ~25+W min from first claim regardless, since `valid_from` never changes on conflict. Postgres `GREATEST` ignores NULLs, so a NULL existing value is safe.

  **c) Keep the no-op; do not RAISE.** A distinct error hands out a "this token is live and owned by someone else" oracle — low value (122-bit space, and a sniffer already knows), but free to avoid. Reusing the existing `'Invalid beacon token'` error would be oracle-free but aborts the whole transaction and would also fire on the **victim's** legitimate reclaim after a squat during the flag-off window, breaking honest old clients. The no-op bounds the residual: a squatted row is resolvable only while `valid_until > now − W` (~36 min), purged within 24 h by cleanup. One honesty note for the header comment: the `WHERE` guards only the **UPDATE** path — the INSERT path (token never claimed) still lets an attacker insert `user_id = attacker` while `enforce_batch_tokens` = 0. That residual belongs to the flag flip, as the audit said; the migration neither fixes nor worsens it, but the comment at `0063:92-97` reads as if the class is closed.

  **d) No accidental alterations.** All three bodies are verbatim outside the marked sites; signatures, `search_path`, and decl blocks match the originals, and there are no GRANT/REVOKE statements — CREATE OR REPLACE preserves ACLs, so 0061's privilege invariant and `security_regression.sql` are untouched. One cosmetic delta: 0063 drops the `COMMENT ON FUNCTION cleanup_ephemeral_data` from `0059:582-583`; the old comment survives (comments are preserved) and is now stale — it doesn't list `beacon_token_batch`. Optional: restate it updated.

  **e) Transactionally fine.** One transaction, three CREATE OR REPLACE = brief `pg_proc` row locks; concurrent `record_sighting`/`claim_token` calls queue for milliseconds. No table DDL. The new DELETE runs at the next scheduled cleanup, not in the migration, and `idx_beacon_token_batch_expiry` (`0031:39`) covers it. Deployment-order caveat: 0063 assumes the 0059/0060 shapes — if prod is still behind (the audit found `proximity-wake` 404ing; 0061's header said prod was at 0055), 0056–0062 must land first or as a unit, which matches your rehearsal shape.

  **One design question to answer deliberately, not a blocker:** the new batch purge sits outside the legal-hold IF/ELSE (`0063:281`), so it purges held users' rows while their `token_claim_history` is preserved — an asymmetry in the hold's evidence chain. The counter-argument is real: `0044:204` (evidence preservation) itself scrubs `beacon_token_batch` as PII, so hold-blind is consistent with existing treatment. Either move it into the `v_holds` branch with `AND NOT public.has_legal_hold(user_id)` for symmetry, or add one comment line stating the hold-blind purge is deliberate because 0044 classifies the table as scrubbable PII.

  Summary of asks before deploy: (1) fold `observer_accuracy_m` into the fallback radius — recommended; (2) hold-aware purge or a deliberateness comment — optional; (3) refresh the stale function comment and note the INSERT-side squat residual in the header — cosmetic.

To resume this session: kimi -r session_7d89f4bc-8080-4bd9-aaf2-66369260923e
