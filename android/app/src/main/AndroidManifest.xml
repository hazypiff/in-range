<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for In Range (BLE + Location for real encounters) -->
    <!-- From the 2026 plan and research: required for background BLE/GPS -->

    <!-- Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Bluetooth classic (Android ≤11 / API 30). Required on Galaxy S9 Android 10. -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Bluetooth (Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- WiFi AP scanning for venue co-location (docs/PROXIMITY_ALGORITHM.md L1).
         Reading cached scan results needs WIFI_STATE + ACCESS_FINE_LOCATION.

         NOTE: usesPermissionFlags="neverForLocation" is deliberately NOT set
         here. That flag is a strong assertion that the app never uses the
         permission to derive physical location, and it would be false: the
         venue matcher scores WiFi AP overlap as a proximity signal
         (_wifiAgreeBoost / _wifiConflictPenalty in venue_matcher.dart), which
         is deriving location. Declaring it would be exactly the kind of
         misrepresentation Play's Data Safety review targets.

         It would also buy nothing: NEARBY_WIFI_DEVICES does not gate
         getScanResults(), which still requires ACCESS_FINE_LOCATION regardless.

         (By contrast BLUETOOTH_SCAN below correctly omits the flag too:
         adding it there would filter beacon-shaped advertisements out of scan
         results and break detection outright.) -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        tools:targetApi="33" />

    <!-- Foreground service for reliable background scanning (critical per research) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Other -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="In Range"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- OAuth / email deep link (Supabase Auth redirect) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="io.inrange.app" android:host="login-callback"/>
            </intent-filter>
        </activity>

        <!-- Foreground service declaration for Beacon scanning -->
        <service
            android:name="id.flutter.background.IsolateHolderService"
            android:foregroundServiceType="connectedDevice|location"
            android:exported="false" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Required for BLE on some devices -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

</manifest>
